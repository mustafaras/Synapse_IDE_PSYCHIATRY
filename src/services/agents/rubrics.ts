export const CRITIC_RUBRIC = `
- Reference evidence explicitly: file path and +/- lines from diff or failing test names.
- If proposing fixes, list "file:line → action: reason".
- If everything passes, end with the single line: READY-TO-APPLY
`;
